#include "Output.h"
#include <iostream>
int main()
{
    do

        break;

    while (1);
    int b = 3;
    int a = (b+=1) / 2;
    setlocale(LC_ALL, "Russian");
    Output file;
    char c[20] = "help.txt";
    file.out(c);
}
